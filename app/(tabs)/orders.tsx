import { Text, View } from "react-native";

export default function Orders() {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: "#0F0F0F",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text style={{ color: "#fff", fontSize: 16 }}>Orders Screen</Text>
    </View>
  );
}
